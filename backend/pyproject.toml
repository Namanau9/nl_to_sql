[project]
name = "nlsql-backend"
version = "0.1.0"
description = "Natural Language to SQL Analytics Assistant - backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
